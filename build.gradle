tasks.register('packageTar') {
    doLast {
        mkdir 'build/dist'
        exec {
            commandLine 'tar', '-cf',
            'build/dist/abinitio-etl-poc-1.0.0.tar',
            'graphs', 'runtime', 'transforms', 'data'
        }
    }
}

build.dependsOn packageTar
