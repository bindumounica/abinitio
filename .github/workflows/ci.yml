name: Ab Initio Style ETL – Gated CI/CD + Scheduler

on:
  push:
    branches: [ "main" ]
    # tags:
    #   - "v*.*.*"
  workflow_dispatch:

env:
  COMPONENT_NAME: abinitio-etl-poc
  IMAGE_NAME: abinitio-etl-poc
  # VERSION: 1.0.0

# =====================================================
# DEV STAGE – BUILD & VALIDATE
# =====================================================
jobs:
  dev:
    name: DEV – Build & Validate
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7

      - name: Derive version from Git tag
        run: |
            VERSION=${GITHUB_REF#refs/tags/v}
            echo "VERSION=$VERSION" >> $GITHUB_ENV

      # -----------------------------
      # DEV GATE: GO MODULE HYGIENE
      # -----------------------------
      - name: Validate Go module dependencies
        run: |
          echo "[CI] Running go mod tidy (validation)"
          go mod tidy

          echo "[CI] Checking for uncommitted changes"
          if ! git diff --quiet; then
            echo "❌ go.mod / go.sum not tidy"
            echo "👉 Run 'go mod tidy' locally and commit changes"
            git diff
            exit 1
          fi       

      # -----------------------------
      # DEV GATE 1: PRE-BUILD
      # -----------------------------
      - name: Pre-build validation
        run: |
          chmod +x ci/pre-build.sh
          ./ci/pre-build.sh

      # -----------------------------
      # DEV GATE 2: RUNTIME VALIDATION
      # -----------------------------
      - name: Run ETL pipeline (DEV validation)
        run: |
          chmod +x runtime/master_pipeline.sh
          ./runtime/master_pipeline.sh

      # -----------------------------
      # DEV GATE 3: BUILD TAR
      # -----------------------------
      - name: Build ETL TAR
        run: |
          chmod +x gradlew
          ./gradlew clean build

      - name: Verify TAR
        run: |
          ls -lh build/dist
          tar -tf build/dist/*.tar

      # -----------------------------
      # DEV GATE 4: BACKWARD COMPAT
      # -----------------------------
      - name: Backward compatibility check
        run: |
          chmod +x ci/check_backward_compatibility.sh
          ./ci/check_backward_compatibility.sh

      # -----------------------------
      # DEV GATE 5: POST-BUILD
      # -----------------------------
      - name: Post-build validation
        env:
          COMPONENT_NAME: abinitio-etl-poc
          VERSION: 1.0.0
        run: |
          chmod +x ci/post-build.sh
          ./ci/post-build.sh

      # -----------------------------
      # DEV GATE 6: SONAR
      # -----------------------------

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: ${{ runner.os }}-gradle
      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: ./gradlew build sonar --info
      - name: Run Go tests
        run: |
          go test ./transforms/... 

      - name: Run Go tests with coverage
        run: |
            go test ./transforms/... -coverprofile=coverage.out
      - name: Sonar Scan
        run: ./gradlew sonar -Dsonar.go.coverage.reportPaths=coverage.out
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Sonar Quality Gate
        uses: sonarsource/sonarqube-quality-gate-action@v1.1.0
        with:
            scanMetadataReportFile: build/sonar/report-task.txt
        env:
            SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          
      # -----------------------------
      # DEV: BUILD & PUSH IMAGE
      # -----------------------------
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push DEV Image
        run: |
            docker build \
              -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-dev \
              -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-sha-${{ github.sha }} \
              .
        
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-dev
            docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}-sha-${{ github.sha }}
        
  

# =====================================================
# UAT STAGE – QUALITY & PROMOTION
# =====================================================
  uat:
    name: UAT – Promote & Validate
    runs-on: ubuntu-latest
    needs: dev
    environment: uat

    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull DEV image
        env:
            DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
            IMAGE_NAME: ${{ env.IMAGE_NAME }}
            TAG: ${{ github.ref_name }}-dev
        run: |
            docker pull "$DOCKER_USER/$IMAGE_NAME:$TAG"
        
  
      - name: UAT Smoke Test
        env:
          DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          TAG: ${{ github.ref_name }}-dev
        run: |
            docker run --rm \
              -e RUN_MODE=UAT \
                "$DOCKER_USER/$IMAGE_NAME:$TAG"
  
      - name: Promote image to UAT
        env:
          DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          TAG_DEV: ${{ github.ref_name }}-dev
          TAG_UAT: ${{ github.ref_name }}-uat
        run: |
            docker tag \
               "$DOCKER_USER/$IMAGE_NAME:-$TAG_DEV"\
              "$DOCKER_USER/$IMAGE_NAME:-$TAG_UAT"
  
            docker push "$DOCKER_USER/$IMAGE_NAME:$TAG_UAT"
  

# =====================================================
# PROD STAGE – MANUAL APPROVAL + RELEASE
# =====================================================
  prod:
    name: PROD – Manual Approval & Release
    runs-on: ubuntu-latest
    needs: uat
    environment: prod   # <-- REQUIRED REVIEWERS ENABLED HERE

    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull UAT image
        env:
            DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
            IMAGE_NAME: ${{ env.IMAGE_NAME }}
            TAG: ${{ github.ref_name }}-uat
        run: |
            docker pull "$DOCKER_USER/$IMAGE_NAME:$TAG"
        
  
      - name: Promote image to PROD
        env:
          DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
            docker tag \
              "$DOCKER_USER/$IMAGE_NAME":-uat \
              "$DOCKER_USER/$IMAGE_NAME":prod
  
            docker push "$DOCKER_USER/$IMAGE_NAME":${{ github.ref_name }}-prod
  

# =====================================================
# PROD SCHEDULED EXECUTION (OPTIONAL)
# =====================================================
  run-prod:
    name: PROD – Scheduled ETL Execution
    runs-on: ubuntu-latest
    needs: prod
    if: github.event_name == 'workflow_dispatch'

    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Run ETL Component (PROD)
        env:
          DOCKER_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
            docker run --rm \
              -e RUN_MODE=PROD \
              -e COMPONENT_NAME=abinitio-etl-poc \
              "$DOCKER_USER/$IMAGE_NAME":${{ github.ref_name }}-prod
  
    # schedule:
    #   - cron: '0 2 * * *'  # Runs daily at 2:00 AM UTC  